---
Static
---
!2 Definitions

!3 Definition for Binary
 * '''binary'''(group)
   * '''recordInfo'''(group, 1-1, noConstraint)
     * '''id'''(textVariable, 0-1, noConstraint, I)
     * '''type'''(recordLink, 0-1, noConstraint)
     * '''validationType'''(recordLink, 1-1, noConstraint)
     * '''dataDivider'''(recordLink, 1-1, noConstraint, P)
     * '''createdBy'''(recordLink, 0-1, noConstraint)
     * '''permissionUnit'''(recordLink, 0-X, noConstraint)
     * '''tsCreated'''(textVariable, 0-1, noConstraint)
     * '''updated'''(group, 0-X, noConstraint)
       * '''updatedBy'''(recordLink, 0-1, noConstraint)
       * '''tsUpdated'''(textVariable, 0-1, noConstraint)
   * '''resourceInfo'''(group, 0-1, noConstraint)
     * '''master'''(group, 1-1, noConstraint)
       * '''resourceId'''(textVariable, 1-1, readWrite)
       * '''resourceLink'''(resourceLink, 1-1, noConstraints)
       * '''fileSize'''(textVariable, 1-1, noConstraints)
       * '''mimeType'''(textVariable, 1-1, noConstraints)
       * '''height'''(textVariable, 1-1, noConstraints)
       * '''width'''(textVariable, 1-1, noConstraints)
       * '''resolution'''(textVariable, 1-1, noConstraints)
     * '''thumbnail'''(group, 0-1, noConstraint)
       * '''resourceId'''(textVariable, 1-1, readWrite)
       * '''resourceLink'''(resourceLink, 1-1, noConstraints)
       * '''fileSize'''(textVariable, 1-1, noConstraints)
       * '''mimeType'''(textVariable, 1-1, noConstraints)
       * '''height'''(textVariable, 1-1, noConstraints)
       * '''width'''(textVariable, 1-1, noConstraints)
       * '''resolution'''(textVariable, 0-1, noConstraints)
   * '''originalFileName'''(textVariable, 1-1, noConstraint)
   * '''expectedFileSize'''(textVariable, 0-1, noConstraints) (only for new)
   * '''expectedChecksum'''(textVariable, 0-1, noConstraints) (only for new)
   * '''expectedMimeType'''(textVariable, 0-1, noConstraints) (only for new)
   * '''checksum'''(textVariable, 0-1, noConstraints)
   * '''checksumType'''(textVariable, 0-1, noConstraints)
   
!3 !-ImageBinary, definition for new-!
 * '''binary'''(group)
   * '''recordInfo'''(group, 1-1, noConstraint)
     * '''validationType'''(recordLink, 1-1, noConstraint)
     * '''dataDivider'''(recordLink, 1-1, noConstraint, P)
   * '''originalFileName'''(textVariable, 1-1, noConstraint)
   * '''expectedFileSize'''(textVariable, 0-1, noConstraints)
   * '''expectedChecksum'''(textVariable, 0-1, noConstraints)
   * '''expectedMimeType'''(textVariable, 0-1, noConstraints)
   
!3 !-ImageBinary, definition for update-!
 * '''binary'''(group)
   * '''recordInfo'''(group, 1-1, noConstraint)
     * '''id'''(textVariable, 1-1, noConstraint, I)
     * '''type'''(recordLink, 1-1, noConstraint)
     * '''validationType'''(recordLink, 1-1, noConstraint)
     * '''dataDivider'''(recordLink, 1-1, noConstraint, P)
     * '''createdBy'''(recordLink, 1-1, noConstraint)
     * '''permissionUnit'''(recordLink, 0-X, noConstraint)
     * '''tsCreated'''(textVariable, 1-1, noConstraint)
     * '''updated'''(group, 1-X, noConstraint)
       * '''updatedBy'''(recordLink, 1-1, noConstraint)
       * '''tsUpdated'''(textVariable, 1-1, noConstraint)
   * '''resourceInfo'''(group, 1-1, noConstraint)
     * '''master'''(group, 1-1, noConstraint)
       * '''resourceId'''(textVariable, 1-1, readWrite)
       * '''resourceLink'''(resourceLink, 1-1, noConstraints)
       * '''fileSize'''(textVariable, 1-1, noConstraints)
       * '''mimeType'''(textVariable, 1-1, noConstraints)
       * '''height'''(textVariable, 1-1, noConstraints)
       * '''width'''(textVariable, 1-1, noConstraints)
       * '''resolution'''(textVariable, 1-1, noConstraints)
     * '''thumbnail'''(group, 0-1, noConstraint)
       * '''resourceId'''(textVariable, 1-1, readWrite)
       * '''resourceLink'''(resourceLink, 1-1, noConstraints)
       * '''fileSize'''(textVariable, 1-1, noConstraints)
       * '''mimeType'''(textVariable, 1-1, noConstraints)
       * '''height'''(textVariable, 1-1, noConstraints)
       * '''width'''(textVariable, 1-1, noConstraints)
       * '''resolution'''(textVariable, 1-1, noConstraints)
   * '''originalFileName'''(textVariable, 1-1, noConstraint)
   * '''checksum'''(textVariable, 1-1, noConstraints)
   * '''checksumType'''(textVariable, 1-1, noConstraints)
